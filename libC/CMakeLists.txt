add_library(libradreturn radRet.cpp)

## if the library should be accessible as an object for other programs e.g. ./lib/liblib_name.a
# install(TARGETS libRadiativeReturn DESTINATION ${CMAKE_CURRENT_SOURCE_DIR}/../lib)
